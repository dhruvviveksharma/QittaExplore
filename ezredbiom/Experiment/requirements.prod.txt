# Production requirements for the ezredbiom container.
# Combines:
#   - ezredbiom/requirements.txt (web stack used by Experiment/backend)
#   - the qiita-spots editable install from the top-level requirements.txt
#     (needed for `from qiita_db.sql_connection import TRN`)
#   - gunicorn for the production WSGI server
#
# Pinned to the same versions as the dev environment to avoid surprises.

# ── Web stack ────────────────────────────────────────────────────────────────
Flask==2.2.5
Flask-Cors==3.0.10
python-dotenv>=1.0.0

# ── LLM client ──────────────────────────────────────────────────────────────
openai>=1.0,<2.0

# ── Legacy / kept for backend imports ───────────────────────────────────────
# tinydb is referenced by sql_store migration code paths.
tinydb>=4.8,<5.0

# ── Data stack (Python 3.9) ─────────────────────────────────────────────────
pandas==1.5.3
numpy==1.26.4

# ── Qiita DB layer ──────────────────────────────────────────────────────────
# Needed by qiita_db.sql_connection.TRN (used in services/study_service.py
# and run.py for PostgreSQL access). Brings transitive deps: psycopg2-binary,
# biom-format, scikit-bio, redis, etc.
psycopg2-binary
redis
biom-format
qiita-files @ https://github.com/qiita-spots/qiita-files/archive/master.zip
-e git+https://github.com/qiita-spots/qiita.git@a34dcebc44ea6408340d31ecaf0efd1f78e3cc6b#egg=qiita_spots

# ── Production WSGI server ──────────────────────────────────────────────────
gunicorn>=21.0,<22.0
